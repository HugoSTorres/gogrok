syntax = "proto3";
package pb;

service Sniffer {
    rpc Connect(SnifferParams) returns (stream Message) {}
}

// A message is just the string form of the HTTP request.
message Message {
    string data = 1;
}

// For now, this one takes nothing
message SnifferParams {}